<flynt-component name="BlockNotFound" class="componentSpacing" {{ options.theme ? 'data-theme=' ~ options.theme }}>
  <div class="container">
    <div class="content" data-size="narrow" data-align="center" data-text-align="center">
      {{ contentHtml|e('wp_kses_post') }}
      <p>
        <a class="button" href="{{ fn('home_url') }}">
          {{ backLinkLabel|e }}
        </a>
      </p>
    </div>
  </div>
</flynt-component>
