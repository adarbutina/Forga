<ul class="menu">
  {% for item in items %}
    <li class="menu-item">
      <a class="menu-link" href="{{ item.url|e('esc_url') }}" {{- item.is_external or item.is_target_blank ? ' target="_blank" rel="noopener noreferrer"' }}>{{ item.title }}</a>
    </li>
  {% endfor %}
</ul>
